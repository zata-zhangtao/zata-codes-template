# E2E 本地运行凭据覆盖文件。
# 复制为 .env.e2e.local 并填入真实值（.env.e2e.local 已被 gitignore，不会提交）。
#
# 默认情况下，凭据从项目根目录的 .env.local 读取：
#   public 用户：APP_BOOTSTRAP_EMAIL / APP_BOOTSTRAP_PASSWORD
#   admin 用户：AUTH_ADMIN_BOOTSTRAP_USERNAME / AUTH_ADMIN_BOOTSTRAP_PASSWORD
# 如果 .env.local 已配置，保留本文件全部注释即可。
#
# URL 默认从 `just run` 写入的 .env.run-state 读取，一般不需要在这里覆盖。
# 如果未使用 `just run`，可手动设置：
#   PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
#   PLAYWRIGHT_ADMIN_BASE_URL=http://localhost:5173
#   PLAYWRIGHT_HEALTH_URL=http://127.0.0.1:8000/health
#
# 如需覆盖凭据，取消下面注释：
# PLAYWRIGHT_IDENTIFIER=user@example.com
# PLAYWRIGHT_PASSWORD=user123
# PLAYWRIGHT_ADMIN_IDENTIFIER=admin
# PLAYWRIGHT_ADMIN_PASSWORD=admin
